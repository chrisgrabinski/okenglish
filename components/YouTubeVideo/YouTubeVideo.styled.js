import styled from "@emotion/styled";

export const Wrapper = styled.div`
  border-radius: 32px;
  overflow: hidden;
  padding-bottom: 56.25%;
  position: relative;

  iframe {
    height: 100%;
    left: 0;
    position: absolute;
    top: 0;
    width: 100%;
  }
`;
